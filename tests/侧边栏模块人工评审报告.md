# 侧边栏模块 - 人工评审报告

**评审日期**：2026-07-28
**评审人**：人工评审
**测试文件**：`tests/suites/test_sidebar.py`
**评审结果**：28/28 通过（4 条修复后通过）

---

## 智能体编排（4 条）

### 第 1 条 `test_workflow_page_loads`

**用例说明**：验证智能体编排页面能正常加载

**评审结论**：✅ 通过

---

### 第 2 条 `test_workflow_has_create_button`

**用例说明**：验证智能体编排页面有新建工作流按钮

**评审结论**：✅ 通过

---

### 第 3 条 `test_workflow_search`

**用例说明**：验证工作流搜索功能可用

**评审结论**：✅ 通过

---

### 第 4 条 `test_workflow_create_and_return`（新增）

**用例说明**：新建工作流 → 进入编辑器 → 返回列表验证

**评审结论**：⚠️ 问题已修复 → ✅ 通过

**评审问题**：
- [x] `get_workflow_count()` 选择器 `[class*='card']` 不匹配真实 DOM（工作流列表是 flex div，不是卡片）
- [x] 409 conflict：残留测试工作流未清理
- [x] conftest `_page_error_monitor` 捕获导航瞬态网络错误

**修复措施**：
1. 移除 `get_workflow_count()` 依赖，改用 UI 文本验证（`span` 含工作流名称）+ API 验证（`GET /web/workflow-defs`）双保险
2. 测试前增加 API 清理残留同名工作流逻辑
3. conftest 白名单增加 `AgentPanelLayout` 轮询请求导航瞬态错误

---

## 记忆（4 条）

### 第 5-8 条 `test_memory_*`

**用例说明**：页面加载、Tab 存在、Tab 切换、Tab 遍历

**评审结论**：4 条全部 ✅ 通过

---

## 知识库（3 条）

### 第 9-11 条 `test_knowledge_base_*`

**用例说明**：页面加载、新建按钮、搜索功能

**评审结论**：3 条全部 ✅ 通过

---

## 定时任务（3 条）

### 第 12-14 条 `test_tasks_*`

**用例说明**：页面加载、表格存在、筛选 Tab

**评审结论**：3 条全部 ✅ 通过

---

## 组织（2 条）

### 第 15-16 条 `test_organization_*`

**用例说明**：页面加载、成员区域

**评审结论**：2 条全部 ✅ 通过

---

## API Key（3 条）

### 第 17-19 条 `test_apikey_*`

**用例说明**：页面加载、创建按钮、搜索功能

**评审结论**：3 条全部 ✅ 通过

---

## 导航可达性（8 条）

### 第 20-27 条 `test_sidebar_nav_*`

**用例说明**：验证 8 个侧边栏菜单项点击后可达

**评审结论**：⚠️ 问题已修复 → 8 条全部 ✅ 通过

**评审问题**：
- [x] 单独运行时用例失败：`logged_in_page` fixture 登录后未等侧边栏渲染完就返回
- [x] 菜单名称与实际不一致："垂直模型库"应为"企业垂直大模型"，"Agent Sites"应为"AOS应用部署"
- [x] conftest 捕获 React 路由瞬态 JS 错误 `Group _r_N_ not found`

**修复措施**：
1. `logged_in_page` fixture 登录后增加 `wait_for_selector("button.agent-sidebar-nav-item")` 等待侧边栏就绪
2. 修正菜单名称匹配真实侧边栏
3. conftest 白名单增加 `Group ... not found` 降级为警告

---

## 侧边栏结构（1 条）

### 第 28 条 `test_sidebar_structure_integrity`

**用例说明**：菜单项数量 + 核心菜单存在性

**评审结论**：⚠️ 问题已修复 → ✅ 通过

**评审问题**：
- [x] 多余的 `WorkflowPage.goto()` 导航步骤（fixture 修复后不再需要）

**修复措施**：
1. 移除多余的前置导航步骤

---

## 全局问题

1. **`logged_in_page` fixture 缺少侧边栏等待**：登录后直接返回 page，侧边栏可能未渲染。已修复为等待 `button.agent-sidebar-nav-item` 出现后再返回。
2. **conftest 白名单不完善**：缺少 `AgentPanelLayout` 轮询导航瞬态错误和 React 路由分组瞬态错误。已补充。
3. **菜单名称过时**：`SidebarNavigation.NAV_ITEMS` 中"垂直模型库"和"Agent Sites"与真实侧边栏不一致。测试用例已修正。

---

## 修改文件清单

| 文件 | 修改内容 |
|------|---------|
| `tests/conftest.py` | `logged_in_page` 增加侧边栏等待；白名单增加导航瞬态错误和 React 分组错误 |
| `tests/suites/test_sidebar.py` | 28 条用例 docstring 标记评审状态；新增 `test_workflow_create_and_return`；修正导航测试菜单名称；移除多余前置导航；`test_sidebar_structure_integrity` 移除多余 goto |
