FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY backend/ backend/
COPY engine/ engine/

EXPOSE 8111

CMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8111"]
