FROM oven/bun:latest
WORKDIR /app
# 测试代码和 FenixAgent 源码通过 volume 挂载
# tsconfig paths (@fenix/*) 解析到 ./app/src/*
CMD ["sh", "-c", "mkdir -p /app/tests/results && cd /app/tests && bun test --reporter=junit --reporter-outfile=/app/tests/results/unit-junit.xml"]
